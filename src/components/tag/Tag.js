const { css } = require("styled-components");

export const baseTagStyles = css`
  font-size: 13px;
  font-weight: 700;
  color: var(--primary);
  background-color: var(--grayish-cyan);
  border: none;
  border-radius: 4px;

  padding: 8px;
`;
